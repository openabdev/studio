[package]
name = "acp-tunnel"
version = "0.1.0"
edition = "2021"
description = "Studio-side reverse-MCP-over-ACP protocol: the /acp session lifecycle and tunnel framing Studio speaks as an ACP WebSocket client (reverse-MCP client ADR, Part B)"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
